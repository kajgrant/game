{
    "tasks": [
      {
        "type": "shell",
        "label": "build & debug main",
        "command": "g++",
        "args": [
          "-std=c++11",
          "main.cpp",
          "-o",
          "main.exe"
        ],
        "options": {
          "cwd": "${workspaceFolder}"
        }
      },
      {
        "type": "shell",
        "label": "build & run main",
        "command": "g++",
        "args": [
          "-std=c++11",
          "main.cpp",
          "-o",
          "main.exe"
        ],
        "options": {
          "cwd": "${workspaceFolder}"
        }
      }
    ],
    "version":"2.0.0"
  }